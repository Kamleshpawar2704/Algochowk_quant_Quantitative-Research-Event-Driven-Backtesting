# Data

Run from the repository root:

```bash
python scripts/download_data.py
```

The command creates `nifty50_daily.csv` from Yahoo Finance `^NSEI`. The CSV is intentionally generated rather than hand-edited. Do not modify the downloaded price series before running the research engine.
